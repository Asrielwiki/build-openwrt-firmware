name: Build OpenWrt Toolchain

on:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/coolsnowwolf/lede
  REPO_BRANCH: master
  # 明确定义种子配置文件变量
  SEED_CONFIG: seed.config
  # 引入 DIY 脚本变量
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  TZ: Asia/Shanghai

jobs:
  build_toolchain:
    runs-on: ubuntu-22.04
    steps:
      - name: Remove all docker images
        run: |
         images=$(docker images -q)
         if [ -n "$images" ]; then
           docker rmi $images
         else
           echo "No images to remove."
         fi
         
      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
             # this might remove tools that are actually needed,
             # if set to "true" but frees about 6 GB
             tool-cache: true
             # all of these default to true, but feel free to set to
             # "false" if necessary for your workflow
             android: true
             dotnet: true
             haskell: true
             large-packages: true
             swap-storage: true
             
      - name: Checkout
        uses: actions/checkout@v4

      - name: Initialization environment
        run: |
          sudo -E apt update
          sudo apt full-upgrade -y
          sudo apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
          bzip2 ccache clang cmake cpio curl device-tree-compiler flex gawk gcc-multilib g++-multilib gettext \
          genisoimage git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev \
          libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev \
          libreadline-dev libssl-dev libtool llvm lrzsz libnsl-dev ninja-build p7zip p7zip-full patch pkgconf \
          python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion \
          swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
          sudo timedatectl set-timezone "Asia/Shanghai"

      - name: Clone source code
        run: git clone $REPO_URL -b $REPO_BRANCH openwrt

# --- 关键修正：执行 DIY-P1 以引入 helloworld 等 feeds ---
      - name: Load Custom Feeds
        run: |
          chmod +x $DIY_P1_SH
          cd openwrt
          $GITHUB_WORKSPACE/$DIY_P1_SH
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          ./scripts/feeds install -f libxcrypt

      # --- 第一步：根据 seed.config 生成完整 .config 并导出变量 ---
      - name: Generate Full Config & Export Env
        id: parse_config
        run: |
          cd openwrt
          [ -e ../seed.config ] && cp ../seed.config .config
          make defconfig

      # --- 第二步：尝试恢复缓存 ---
      - name: Cache Toolchain & ccache
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            openwrt/staging_dir/toolchain-*
            openwrt/staging_dir/host*
            openwrt/staging_dir/target-*
            ~/.ccache
          key: ${{ runner.os }}-toolchain-${{ hashFiles('seed.config') }}

      # --- 第三步：核心编译（只有缓存失效才执行） ---
      - name: Build Toolchain
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          cd openwrt
          # 编译基础工具链
          make toolchain/install -j$(nproc)

      # --- 第四步：按需预编译（只有缓存失效 且 配置选中 才会执行） ---
      - name: Pre-compile Sing-box & Dependencies
        run: |
          # 1. 进入目录
          cd openwrt
  
          # 2. 定义目标关键词
          KEYWORDS="shadowsocks sing-box xray docker wireguard samba tailscale"
          
          for KEY in $KEYWORDS; do
              # 找出所有选中的包
              SELECTED_PKGS=$(grep "^CONFIG_PACKAGE_.*$KEY.*=y" .config | cut -d'=' -f1 | sed 's/CONFIG_PACKAGE_//')
              
              if [ -z "$SELECTED_PKGS" ]; then continue; fi
          
              for PKG in $SELECTED_PKGS; do
                  echo ">>> 正在处理: $PKG"
          
                  # 【安全搜索第一步】存入变量，严禁直接使用管道接 dirname
                  MAKEFILE_FILE=$(grep -rl "PKG_NAME:=$PKG" package feeds 2>/dev/null | head -n 1)
          
                  # 【安全搜索第二步】逻辑判断
                  if [ -n "$MAKEFILE_FILE" ]; then
                      PKG_PATH=$(dirname "$MAKEFILE_FILE")
                  else
                      # 备选方案：按目录名找
                      PKG_PATH=$(find package feeds -name "$PKG" -type d -print -quit)
                  fi
          
                  # 【安全搜索第三步】执行编译
                  if [ -n "$PKG_PATH" ]; then
                    echo ">>> 命中物理路径: $PKG_PATH"
                    
                    # 针对重型插件同步内核环境
                    if [[ "$PKG" =~ (sing-box|xray|shadowsocks|docker) ]]; then
                        make target/compile -j$(nproc)
                    fi
                    
                    make "$PKG_PATH/compile" -j$(nproc) || echo "!!! $PKG 预编译失败，跳过"
                  else
                    # 最后的保底：按关键词模糊找一个目录
                    echo ">>> 警告: 未能精准定位 $PKG，尝试使用关键词 $KEY 模糊寻找..."
                    LUCKY_PATH=$(find package feeds -name "*$KEY*" -type d -print -quit)
                    [ -n "$LUCKY_PATH" ] && make "$LUCKY_PATH/compile" -j$(nproc)
                  fi
              done
          done


#      - name: Pre-compile Xray
#        if: steps.cache-toolchain.outputs.cache-hit != 'true' && steps.parse_config.outputs.HAS_XRAY == 'true'
#        run: |
#          cd openwrt
#          echo "Building Xray-core package..."
#          make package/feeds/custom_repo/xray-core/compile -j$(nproc)

      - name: Final Tools Install
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          cd openwrt
          make tools/install -j$(nproc) || make tools/install -j1 V=s
